# Experiments

This folder contains configurations and scripts for running volatility experiments.

## Structure
- `configs/`: YAML files defining model parameters, data sources, and run options.
- `run.py`: Entry point script for executing experiments with a chosen config.

## Outputs
- Experiment results are generated when running `run.py`
- Figures passed to `paper/figs/` for reporting